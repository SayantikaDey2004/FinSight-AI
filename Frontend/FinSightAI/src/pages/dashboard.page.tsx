export default function DashboardPage() {
  return (
    <div style={{ padding: 24 }}>
      <h1>Dashboard</h1>
      <p>This is a placeholder dashboard to allow the app to build and run.</p>
    </div>
  );
}
